/*! lightweight chart placeholder compatible with new Chart(ctx, config) */
(function () {
  function NoopChart(_ctx, _config) {
    this.destroy = function destroy() {};
  }
  window.Chart = window.Chart || NoopChart;
})();
